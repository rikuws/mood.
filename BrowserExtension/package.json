{
  "name": "pinax-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "mood. Chromium extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
